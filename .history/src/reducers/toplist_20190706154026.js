const LIST_STATE={
    list:[],
    limit:20,
    
}

export default function topiclist(pre=LIST_STATE, action) {
    switch(action.type){
        case '':

        default:
            return LIST_STATE
    }
}